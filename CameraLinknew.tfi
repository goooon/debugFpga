


// Instantiate the module
CameraLinknew instance_name (
    .pClk(pClk), 
    .nrst(nrst), 
    .dat(dat), 
    .syncOrNot(syncOrNot), 
    .sync_out(sync_out), 
    .empty(empty), 
    .mode(mode), 
    .tx(tx), 
    .txclk(txclk), 
    .cc_en(cc_en), 
    .ccen_n(ccen_n), 
    .pwr_dwn_n(pwr_dwn_n), 
    .read_en(read_en)
    );


